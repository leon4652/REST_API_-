<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.ssafy.board.model.mapper.BoardMapper">


<resultMap type="Board" id="boardResultMap">
  <result property="no" column="no"/>
  <result property="hits" column="hits"/>
  <result property="title" column="title"/>
  <result property="contents" column="contents"/>
  <result property="registerTime" column="register_time"/>
  <result property="writerName" column="name"/>
</resultMap>


<select id="selectBoard" resultMap="boardResultMap">
  select b.*, u.name
    from board b LEFT JOIN user u ON b.member_no = u.no
</select>

<select id="getBoard" parameterType="int" resultMap="boardResultMap">
	select *
	  from board
	 where no = #{no}
</select> 

<delete id="deleteBoard" parameterType="int">
	update board
	   set remove_date = now()
	 where no = #{no}
</delete>

<insert id="writeBoard" parameterType="Board">
	insert into board(title, contents, member_no, register_time) 
	values(#{title}, #{contents}, #{memberNo}, now())
</insert>

<update id="updateBoard" parameterType="Board">
	update board
	   set update_date = now(),
	   title = #{title},
	   contents = #{contents}
	 where no = #{no}
</update>


</mapper>