<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.ssafy.board.model.mapper.ShareBoardMapper">

	<insert id="writeShareBoard" parameterType="ShareAttraction">
		insert into share_attraction(share_no, attraction_id, register_time) 
		values(#{shareNo}, #{attractionId}, now())
	</insert>

</mapper>