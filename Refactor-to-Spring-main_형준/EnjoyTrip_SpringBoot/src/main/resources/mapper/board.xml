<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.enjoytrip.board.model.mapper.BoardMapper">


<!-- 
	List<Board> selectArticle(Page page) throws SQLException;
	void writeArticle(Board Board) throws SQLException;
	Board getArticle(int articleNo) throws SQLException;
	void updateHit(int articleNo) throws SQLException;
	void modifyArticle(Board Board) throws SQLException;
	void deleteArticle(int articleNO) throws SQLException;
	int selectArticleCount() throws SQLException;
 -->
 
 
 	
 	<select id="selectArticle" parameterType="page" resultType="board">
 	select article_no articleNo, user_id userId, subject, hit
 	from board
 	order by article_no desc
 	limit #{begin}, #{listSize}
 	</select>
 	
 	<insert id="writeArticle" parameterType="board">
 	insert into board
 	(user_id, subject, content) 
 	values (#{userId}, #{subject}, #{content})
 	</insert>
 	
 	<select id="getArticle" parameterType="int" resultType="board">
 	select subject, article_no articleNo, user_id userId, content, hit, register_time registerTime
 	from board
 	where article_no = #{articleNo}
 	</select>
 	
 	<update id="updateHit" parameterType="int">
 	update board
 	set hit = hit + 1
 	where article_no = #{articleNo}
 	</update>
 	
 	<update id="modifyArticle" parameterType="board">
 	update board
 	set subject = #{subject}, content = #{content}
 	where article_no = #{articleNo}
 	</update>
 	
 	<delete id="deleteArticle" parameterType="int">
 	delete from board
 	where article_no = #{articleNo}
 	</delete>
 	
 	<select id="selectArticleCount" resultType="int">
 	select count(*)
 	from board
 	</select>
</mapper>